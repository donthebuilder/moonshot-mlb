# Moonshot

The MLB HR dashboard on Replit: the Streamlit app as it stands, restyled to
match the original Next.js dashboard UI.

Reads the published `data` branch of MLB-HR-DASHBOARD-STREAMLIT. No bot here,
no workflows -- the two repos cannot interfere.
